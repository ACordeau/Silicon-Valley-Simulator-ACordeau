package main.java.svalley;

public class Google extends Startup{
    
    Google(){
        setHealth(10);
        setCurrentHealth(getHealth());
        setAttack(2);
        setDefense(1);
        setType("BROWSER");
    }

}
